This is a template window with all related subframes.  You can create a
full window that is a duplicate of the

PyMono.gui.forms.modules.preferences.connection_module

by simply replacing the "sample" in the structure by whatever you want to call
your new window, frames and notebooks.